name: tfrun
description: Manage terraform collaboration
author: Digger

runs:
  using: "composite"
  steps:
    - run: go run ${{github.action_path}}/main.go
      shell: bash

branding:
  icon: globe
  color: purple