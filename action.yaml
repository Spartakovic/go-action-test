name: tfrun
description: Manage terraform collaboration
author: Digger

runs:
  using: "composite"
  steps:
    - run: |
        go run ${{github.action_path}}
      shell: bash
    - run: tfrun
      shell: bash

branding:
  icon: globe
  color: purple